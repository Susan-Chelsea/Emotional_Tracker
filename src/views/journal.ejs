<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Journal</title>
</head>

<body>
    <main class="bg-white px-6 py-32 lg:px-8">
        <div class="mx-auto max-w-3xl text-base leading-7 text-gray-700">
            <p class="text-base font-semibold leading-7 text-indigo-600">
                Journal for
                <%=new Date(journal.timestamp).toLocaleDateString()%>
                    submitted at
                    <%=new Date(journal.timestamp).toLocaleTimeString()%>
            </p>

            <h1 class="mt-2 text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
                A Journal title
            </h1>

            <ul role="list" class="mt-8 max-w-xl space-y-8 text-gray-600">
                <li class="flex gap-x-3">
                    <svg class="mt-1 h-5 w-5 flex-none text-indigo-600" viewBox="0 0 20 20" fill="currentColor"
                        aria-hidden="true">
                        <path fill-rule="evenodd"
                            d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z"
                            clip-rule="evenodd" />
                    </svg>
                    <span>
                        <strong class="font-semibold text-gray-900">
                            <%=journal.emotion%>
                        </strong>
                        <%=journal.trigger%>
                    </span>
                </li>
            </ul>
            <section id="journal-notes" class="pt-5 mt-2 ">
                <h3 class="text-xl font-bold tracking-tight text-gray-900 mb-2">Notes</h3>
                <p>
                    <%=journal.notes%>
                </p>
            </section>
    </main>
</body>

</html>